# World import evidence

Issue #7 tracks the complete importer. The desktop and browser workspace read world archives, preserve their configuration facts, and reconstruct capacity goals for supported assigned machines. The Imported factory dialog lets the player correct recipe assignments and retained outputs. Unassigned and unsupported machines remain visible.

`kernel/world.js` reads ZIP directory entries individually, verifies their checksums, and decodes region chunks and big-endian NBT. It retains signed 64-bit NBT values as decimal strings. It recognizes wrapper directories, the vanilla dimensions, and namespaced dimension folders. Malformed chunks and block entities produce entries in the import report rather than deleting successfully read machines. Current limits are 256 MiB for either the compressed or expanded size of a required archive entry, 32 MiB per expanded chunk, 128 NBT nesting levels, and two million NBT collection entries. Directory metadata is limited to 64 MiB and 250,000 entries. ZIP64 directory records and extended entry fields are supported. Region chunks support gzip, zlib, raw NBT, and Minecraft’s Java LZ4 block stream. Split and encrypted archives remain unsupported.

The desktop reader keeps an open file descriptor; the browser worker reads ranges from the selected File with `FileReaderSync`. Neither path loads the entire ZIP. Metadata reads use a small cached window. Regions are expanded individually, and decoded chunks are consumed one at a time. Geometry matching keeps four chunks and one region in its cache. Recovered machine facts still need memory in proportion to the imported factory. The original archive is never written. Canceling closes the worker or desktop process and its file handles.

Run `node tools/verify_archive_ranges.mjs <world.zip> <catalog.json.gz> <report.json>` for desktop range parity, and `node tools/verify_archive_browser.mjs <world.zip> <catalog.json.gz> <report.json>` for the selected-File worker path. To check a large archive, use `python tools/extraction/pad_world_archive.py <world.zip> <large.zip> --mib 512` and supply the original ZIP as the fourth argument to either verification tool. That compares the recovered factory while proving unrelated padding is not read. These are reader checks; the exported application checks remain separate.

The controlled array world with 512 MiB of unrelated padding imports as the same twelve machines and seven capacity goals. Both range readers request 1,819,117 bytes from the 537,497,949-byte archive. The standalone Windows export passes `node tools/verify_desktop_world.mjs <large.zip>` with an empty PATH. The embedded browser export passes `node tools/verify_app_browser.mjs <large.zip> <catalog.json> <catalog.json> arrays`, including persistence and portable export. `node tools/verify_import_recovery_browser.mjs` checks cancellation, malformed-ZIP recovery, and another goal after failure. Padding tests establish bounded archive reads; they do not simulate a world containing millions of installed machines.

The machine adapter reads `activeRecipe`, `upgradesItemStack`, `machinesStack`, `activeShape`, and `casing` from the matching MI and addon serialization. The contained machine determines an array's recipe type. Unrecognized mod block entities retain their coordinates and raw facts for later adapters or correction. These facts establish saved configuration, not measured throughput.

The replicator adapter reads its first `items` entry's resource key and amount. A nonempty default-stack template selects the corresponding verified replication route and records that the template has been obtained. It does not become a free input supply. Component-bearing templates without matching rules remain unresolved. The AE2 molecular assembler reads the dedicated pattern in `inv.item10`, speed cards from `upgrades`, and an active forced pattern from `myPlan` when present. Forced patterns retain a separate evidence label because they may be temporary network work. A dedicated pattern with substitutions disabled pins its actual ingredient choice. Conflicting fixed choices remain unresolved rather than silently combining incompatible targets.

The AE2 adapter follows version 19.2.17, source tag `neoforge/v19.2.17`, commit `79ee2c704ad62941a426c26b1cb1f76ef5b2ee5a`. `PatternProviderLogic`, `AppEngInternalInventory`, `CableBusContainer`, `EncodedProcessingPattern`, `EncodedCraftingPattern`, and `GenericStack` define the fields. Pattern inventories contain item stacks with `Slot` and encoded components. Cable parts are stored under direction names. Block providers use the block-state `push_direction` property. Processing patterns carry `sparseInputs` and `sparseOutputs`, with AE resource type `#t` and quantity `#`.

Patterns remain provider configuration. They do not create production machines. An unassigned adjacent machine receives an inferred recipe only when its recipe type and the pattern's material quantities match a single normalized or compiled process. Multiple matches remain ambiguous. Saved active recipes take precedence. Patterns beside a uniquely associated input hatch also provide controller candidates. Remote networks and additional machine serialization formats still need adapters and verification.

`kernel/reconstruct.js` compiles saved loadouts with the same capacity rules as ordinary goals. It groups identical configurations while retaining each origin. Its initial inference keeps primary outputs with no other assigned consumer as capacity goals. Material links are deductions, not a recovered cable network. Cycles and byproduct retention can need corrections. Only retained end-goal configurations constrain the new request; an imported upstream bottleneck does not constrain the recalculated factory. Requester quantities stay separate.

With the portable catalog, the controlled world's macerator reconstructs one machine with eight advanced upgrades and a configured capacity of 40 copper dust per second. The three unassigned controllers remain unresolved. The browser application imported that real ZIP, preserved the goal, allowed its removal in the review dialog, and persisted the correction. The fixture controllers are unformed, so this establishes saved configuration and reconstruction, not a functioning multiblock installation.

The runtime probe identifies 52 multiblock-part types through the actual hatch class. The importer retains these parts separately from production machines. It also captures 60 machines' active-shape templates, including positions, allowed hatch types, and shared member rules. For MI’s four state-only member factories, the probe tests every registered block state. Other predicates remain explicitly unresolved. The importer applies the saved controller orientation, selected shape, and actual saved blocks. Missing members and competing hatch claims prevent automatic association. A matching geometry is a deduction; it does not prove that a powered machine was operating.

The isolated saved MI reactor at overworld (832, 160, 0) imports as one controller with five matched nuclear hatches and no structural problems. Its saved state has no unique supported recipe assignment, so reconstruction leaves it unresolved instead of inferring a power rate from the installed fuel. Run `node tools/verify_nuclear_world.mjs <private-reactor-world.zip> data/statech-2.0.1.json.gz data/provenance/nuclear-reactor-report.json` to repeat this check. The world hash is in the report; the ZIP stays outside Git.

The released pack has one projected directional state predicate: vertical chains in steam quarries. The probe calls MI's loaded world and template rotations for every accepted state at two positions, then records the four resulting world-state projections. `tools/verify_rotation_catalog.mjs` checks that mapping in the bundled catalog. `tools/verify_rotation_world.mjs` checks a controlled save containing north-, east-, south-, and west-facing quarries. The loaded matcher accepted each placed structure with four vertical chains; the importer matched all four saved geometries. Before this rotation mapping, the same save left the three non-south quarries unresolved. Unknown directional rotations remain explicit unresolved entries.

ME Requester 1.21.1-1.5.0 stores indexed entries under `requests`. Each entry has a resource `key`, enabled `state`, stock `amount`, and crafting `batch`. The adapter follows tag `v1.21.1-neoforge-1.5.0`, commit `57554ba7e126b38e4a38f19c37cf9916dfef48bc`. It preserves long quantities as decimal strings and leaves their rate unset. Neither a stock threshold nor a crafting batch establishes a refill interval.

## Reproduce the controlled world check

Use the isolated instance described in [extraction evidence](extraction.md). Run the capture tool with `--fixture`, or apply `tools/extraction/fixture-commands.txt` after startup. The commands configure six machines at y=100. They do not form the multiblocks or measure production. The Java fixture command places both AE2 provider forms with a copper-cluster processing pattern, a separate input hatch, and an ME Requester configured for 4,096 copper dust with crafting batch 64. It also creates a replicator with one iron-ingot template and a molecular assembler with a dedicated oak-plank-to-stick pattern and two speed cards. It uses the mods' inventory and component APIs. Save and stop the server before archiving its world with `python tools/extraction/archive_fixture.py --world <world-directory> --output <world.zip>`.

Run `node tools/verify_world_capture.mjs <world.zip> <machines.json> <report.json>`. The fixture check requires the macerator's assigned recipe and eight advanced upgrades, an unassigned assembler, a singleblock array containing 16 macerators and four turbo upgrades, and a multiblock array containing eight distillation towers. It also checks both provider directions, the separate hatch, and the requester's exact quantities without an invented rate. Keep the world archive outside Git. The compact report records its digest and selected facts.

Run `node tools/verify_kernel_browser.mjs <world.zip> <machines.json>` to compare the actual archive's import in Node and a browser Worker inside a cross-origin iframe. This is an isolated computation test, not verification of an exported Godot application. Run `npm test` for synthetic NBT, ZIP, region, AE2, capacity, and planning regressions.

Pass the compiled player catalog in place of the raw machine capture to verify the extended six-machine fixture and reconstruct its three capacity goals. `node tools/verify_real_catalog.mjs <player-catalog.json> <world.zip>` checks 40 copper dust/s, 1 replicated iron ingot/s with 100 mB/s UU matter, and 80/6 sticks/s with 40/6 oak planks/s. These are independently derived configured capacities. `node tools/verify_app_browser.mjs <world.zip> <player-catalog.json> <player-catalog.json> extended` exercises the newer fixture in the exported application, including retained template and ingredient settings.

## Archive and compression checks

ZIP64 parsing follows the directory records and extended entry fields in the [PKWARE ZIP specification](https://pkware.cachefly.net/webdocs/casestudies/APPNOTE.TXT). The controlled world was archived with 65,536 entries using Python’s ZIP writer and imported through the exported browser application. Reproduce that directory form with `archive_fixture.py --zip64-directory`. The added empty files exist only in the archive.

LZ4 parsing follows [lz4-java 1.8.0](https://github.com/lz4/lz4-java/tree/1.8.0), including its block headers and masked XXHash32 checksum. Set `region-file-compression=lz4` in the isolated server before saving the fixture. The verified archive contains 81 LZ4 chunks alongside 760 unchanged zlib chunks. Node, browser Worker, and exported Godot browser imports recovered all six configured machines. The exported application also passed plan persistence and editing checks. Small streams produced by the same Java library cover raw and compressed blocks; regression checks reject invalid offsets, checksums, truncation, and output beyond the import limit.

## Controller and hatch fixture

The irradiator adapter uses a uniquely matched structure, active rods in the first nuclear-hatch slots, and the source input inventory. A uniform fuel and source identifies the captured route and installed hatch count. Empty hatches, mixed fuels, mixed source inputs, and ambiguous structures remain unresolved with a focused correction message. Depleted output does not identify the original rod configuration. The imported rate uses the full replacement cycle, not the remaining life of the saved rod. Ongoing neutron-source consumption also establishes a dependency when inferring end goals.

The irradiation structural probe leaves eight fresh uranium rods installed after verifying its completed cycle. Archive this stopped world and run `node tools/verify_irradiation_capture.mjs <active-world.zip> <player-catalog.json> <report.json> <completed-world.zip>`. The last archive contains the same controller with depleted outputs and no active rods. The check compares these two real saves: the active setup must reconstruct one eight-hatch capacity goal at 0.02 rods per second; the completed setup must remain unresolved.

Run the capture tool with `--structure-fixture` to create an east-facing electric blast furnace at `(64, 100, 0)`, an item input hatch at `(64, 100, 1)`, and a pattern provider at `(65, 100, 1)`. The probe places the current shape and requires the loaded MI `ShapeMatcher` to accept it before saving. The controller has no active recipe; the provider contains the pack’s uncooked-steel-to-steel pattern.

Archive the stopped world and run `node tools/verify_structure_capture.mjs <world.zip> <player-catalog.json> <report.json>`. The check requires the saved hatch relationship and inferred recipe, alongside four reconstructed capacity goals. Shared hatches and missing blocks have separate regression coverage. `verify_app_browser.mjs <world.zip> <player-catalog.json> <player-catalog.json> structure` exercises this fixture in the exported application.

## Tesla infrastructure

Matched Tesla towers become infrastructure configurations rather than recipe goals. The importer uses the saved winding index and uniquely associated energy-input hatches. Each tower keeps its world coordinates and a separate configuration ID, so different input and receiver tiers remain independent. Its winding transfer limit becomes an editable capacity target, and the planner may resize support hatches. Saved energy remains a quantity. Receiver coverage and actual transmission are not established by this import.

Without a redstone module, the initial plan assumes continuous enabled operation with supplied energy. A saved redstone module requires the player to confirm that assumption in Imported factory. Turning off the infrastructure option excludes its operating demand. Unsupported upgrades, unknown tiers, incomplete structures, and ambiguous shared hatches remain visible as unresolved entries.

Run `node tools/verify_tesla_capture.mjs <world.zip> <player-catalog.json> <report.json> [inspection.json]` against the stopped fixture world. The optional inspection file contains private world data and belongs outside the repository. The compact report in `data/provenance/tesla-save-report.json` records the archive digest, copper winding, seven LV input hatches, and 64 EU/t enabled drain. Use `verify_app_browser.mjs <world.zip> <player-catalog.json> <player-catalog.json> tesla` for the ten-machine fixture in the exported application.

The same world contains two zero-byte region files. Minecraft 1.21.1's matching `RegionFile` constructor opens files with CREATE, initializes a zeroed header, and accepts an immediate end-of-file read. The importer likewise treats a zero-byte region as having no chunks. Partial headers and invalid sector lengths still report errors; they are not treated as empty regions.

## Charged storage units

The controlled `--storage-fixture` world places all five MI storage tiers at distinct coordinates and charges each to one-third capacity before saving. `node tools/verify_storage_world.mjs <world.zip> <fixture.json> data/statech-2.0.1.json.gz <report.json>` checks the actual ZIP: all five block entities become dedicated storage results with exact decimal `storedEu`, capacity, transfer limits, and origins. No recipe correction or production goal is invented. The compact result is `data/provenance/storage-world-report.json`. Missing or invalid charge, or a storage tier absent from the dataset, remains a focused unresolved entry.

The application includes each recognized unit by default and lets the player exclude it in Imported factory. Enabled units set exact tier counts in Power storage and appear in the shared graph with their saved charge and origin. A saved charge is startup stock; the solver still sizes sustained generation and reports its own planned initial charge. Importing storage without a generation goal creates storage nodes but no invented power output. `tools/test_storage_import_ui.gd -- --world=<world.zip>` exercises the real ZIP, correction, undo, settings, graph, and a subsequent solar plan. `tools/verify_storage_desktop.mjs <world.zip>` and `tools/verify_storage_app_browser.mjs <world.zip>` check the extracted desktop and embedded browser packages. The clear-weather solar gap adapter currently accepts one selected tier, so a player importing several tiers must choose one when planning solar generation.

## Saved solar panels

The controlled solar world has LV, MV, and HV panels at `(400, 250, 0)`, `(408, 250, 0)`, and `(416, 250, 0)` in the overworld. Each saved panel has one matching photovoltaic cell with four wear ticks and nine millibuckets of distilled water. The importer reads these block entities and selects the water-assisted route from the saved fluid. A missing or incompatible cell remains unresolved until the player confirms that a matching cell can be fitted and kept supplied. The review dialog offers that focused correction, can exclude a panel, and can switch it to the dry route. Confirmation records player evidence but never invents a saved cell or an ongoing supply. A different saved item stays visible as `other_saved_item`. The dialog and inspector show the saved facts and origin separately from assumed ongoing supplies. A saved quantity does not prove a production rate.

Enabled panels set exact counts in `request.installed` and enter the Power graph without an invented electricity goal. Factory settings → Power sources lets the player change those counts. When the imported plan has no selected storage tier, it adds the smallest tier whose discharge can cover the combined panel peaks; this is planned support, not a saved storage unit. The inspector shows each panel's world origin and stocks. The calculation still assumes clear weather, open sky, a free output path, and continuing cell and water supplies. A dry-route correction keeps the node's manual position and can be undone.

Run `node tools/verify_solar_world.mjs <world.zip> <solar-panels.json> data/statech-2.0.1.json.gz data/provenance/solar-world-report.json` against the stopped controlled world. The report records source hashes and selected facts but contains no world archive. `tools/test_solar_world_ui.gd -- --world=<world.zip>` checks the native graph, review, settings, corrections, and undo. `tools/test_solar_cell_ui.gd` checks the missing-cell confirmation and keeps it distinct from the saved stock. `node tools/verify_solar_desktop.mjs <world.zip>` and `node tools/verify_solar_world_app_browser.mjs <world.zip>` check the standalone and embedded exports with player-configured external cell and water supplies. For a missing-cell case, `node tools/verify_solar_cell_app_browser.mjs` imports a focused portable fixture and writes a confirmed plan under `.plans/artifacts/solar-cell/`; `node tools/verify_solar_cell_desktop.mjs <confirmed-plan.json>` checks that plan in the standalone export. The source ZIP remains private and unchanged. The measured generation recipe and matching source capture are described in [extraction evidence](extraction.md).

## Saved blast furnaces

The released 1.21.1 server saves vanilla blast-furnace stacks under `Items` and progress under `CookTime`, `CookTimeTotal`, and `BurnTime`. `RecipesUsed` records past crafts. The importer matches a supported blasting route only when a saved input, active cooking progress, queued fuel, and cook time identify one route. The queued fuel selects a full future burn; the fuel that is already burning may differ. Output stock and recipe history do not create production goals. Missing fuel, history-only furnaces, item components, and conflicting slots remain visible for correction. The review dialog shows those saved facts and places input-matched routes first. Inferred retained outputs use one-machine capacity targets with continuous input supply, not observed output rates.

Use a fresh isolated world and the matching probe, then run `python tools/extraction/run_capture.py --instance <instance> --jdk <jdk21> --blasting-save-fixture`. The probe leaves four furnaces at `(600, 160, 0)` through `(612, 160, 0)`: active coal, active lava bucket, history without an input, and active cooking without queued fuel. It checks the real saved NBT before shutdown. Archive the stopped world with `python tools/extraction/archive_fixture.py --world <world-directory> --output <world.zip>`. Keep the archive and capture outside Git.

Run `node tools/verify_blasting_world.mjs <world.zip> <blasting-save-fixture.json> data/statech-2.0.1.json.gz <report.json> [private-world-result.json]`. The tracked `data/provenance/blasting-save-report.json` records only input hashes and selected facts. Two active furnaces reconstruct capacity goals; the other two need focused corrections. Choosing coal for the empty-fuel furnace combines it with the coal goal at two installed machines. Use the optional private result with `tools/test_blasting_world_ui.gd -- <private-world-result.json>` to check and render the review dialog. `node tools/verify_archive_ranges.mjs` and `node tools/verify_archive_browser.mjs` check bounded desktop and selected-File browser reads. After exporting, `node tools/verify_blasting_desktop.mjs <world.zip>` checks a recalculated graph in the standalone Windows build with an empty PATH, and `node tools/verify_blasting_app_browser.mjs <world.zip>` checks ZIP import and persistence in the embedded web build. These application checks configure pure iron and fuel as explicit external supplies. The bundled catalog has no verified replicatorless production route for Spectrum pure resources yet; issue #49 tracks that gap.

## Formed processing arrays

The fixture includes a formed Extended Industrialization array with eight electric macerators and an Industrialization Overdrive array with eight distillation towers. Both use four advanced upgrades and the smallest structure. Real controller ticks receive continuous hatch inputs and power, with outputs removed after each completion. After warm-up, the macerators produce 48 copper dust in ten ticks using 3,200 EU. The distillation array consumes 8,000 mB of crude oil and produces 8,000 mB of combined fractions in 53 ticks using 57,600 EU. The first recorded completion crosses the warm-up boundary; the following three establish the steady interval.

Both saved controllers retain the installed machines, upgrades, and actual active recipe. `data/provenance/array-save-report.json` records their origins and archive digest. The two structure-check reports record placed blocks and measured batches. The older unformed array fixtures remain unresolved rather than being counted as additional installed production.

Prepare each fixture with `node tools/prepare_array_fixture.mjs <player-catalog.json> <private-instance>/planner-structure-bill.json single` or `multi`, then run the capture with `--structure-bill`. Archive the stopped world and run `node tools/verify_array_capture.mjs <world.zip> <player-catalog.json> <report.json>`. `node tools/verify_array_catalog.mjs` checks the bundled data against the recorded arithmetic and bills without game files. Use the `arrays` mode of `verify_app_browser.mjs` for the twelve-machine world in the exported application.
