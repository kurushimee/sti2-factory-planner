# Runtime extraction evidence

Waste collector probes run each installed tier with zero, one, and two cows in a temporary empty test area. Each case uses 600 actual machine ticks with continuous power and output removal. Bronze, steel, and electric collectors deliver 500, 1,000, and 2,000 mB at ticks 300 and 600. They consume 1 and 2 mB of steam or 4 EU per active tick. No animal means no output or consumption; a second animal does not increase production. The probe removes its cows and machine afterward. Planner routes preserve a live animal as startup stock and state its placement requirement. They assume full rated power and do not exploit partial-energy delivery behavior.

Issue #3 covers the complete dataset and mechanics. The tools here capture evidence from the released pack; they do not yet produce a complete planner dataset.

## Reference inputs

The CurseForge 2.0.1 client archive and the GitHub 2.0.1 client release have identical SHA-256 hashes: `7c39a9cd56b52e320fdfdb72300bc3317f51bf02feaec0a358a46f3123deaa4d`. The GitHub server archive is `7bf868078e1c403b1a264baeafab8c5e443f2ffbc33484347438fc894e45ce8f`. The manifest specifies Minecraft 1.21.1 and NeoForge 21.1.250. `data/provenance/sources.json` records archive and source revision identities. `statech-2.0.1-inputs.json` records every released server mod archive, declared version and license, and every pack script and configuration input hash.

The release contains MI 2.5.8, EI 1.16.2, IO 1.14.0, YAI 1.6.3, MI Tweaks 1.9.5, Tesseract API 1.12.16, StaTech Companion 1.0.3, and AE2 19.2.17. Source checkouts are pinned; the IO and YAI source heads declare the matching versions but still need bytecode comparisons for each calculation rule used.

## Reproduce the capture

Keep downloads and worlds outside the repository, such as `F:/sti2-work`. Download the client and server archives from the recorded release, unpack both, and make a separate working copy of the server. Install Java 21 and NeoForge 21.1.250 in that working copy using the official installer. Bind the test server to localhost. Do not use a player's existing world for instrumentation.

Run `python tools/extraction/inventory.py --server <unmodified-server-directory> --client <unmodified-client-directory> --output <inventory.json>` before modifying the instance. The output must match the pinned inventory.

Copy `tools/extraction/runtime_export.js` to the isolated instance's `kubejs/server_scripts/zz_planner_export.js`. Run `python tools/extraction/build_probe.py --instance <instance> --jdk <jdk-directory> --work <private-build-directory>`, then copy the resulting `planner-probe.jar` into the instance's `mods` folder. Restart the server after changing the Java probe.

After startup and all runtime recipe injection have finished, run `planner_export` and `planner_probe` in the server console. KubeJS script changes require `reload` before running the export again. Read `planner-extraction/runtime.json` and `planner-extraction/machines.json`. A success line is not sufficient: inspect the failure arrays, expected counts, and arithmetic samples. Keep the complete console log privately to distinguish pack warnings from extraction failures.

For a prepared Windows instance, `python tools/extraction/run_capture.py --instance <instance> --jdk <jdk-directory>` starts the server, waits for startup, runs both commands, and stops it. Run `python tools/extraction/verify_capture.py --capture <instance>/planner-extraction --output <report.json>` to validate the pinned counts, tag membership, array eligibility, and arithmetic. Use `--compare <previous-runtime.json>` to require a repeated capture to have identical content after canonical ordering. The compact report records actual loaded mod versions, including nested dependencies. Run the extraction regression checks with `python -m unittest discover -s tools/extraction -p 'test_*.py'`.

Use `--certus-farm --rotation-fixture` when refreshing the bundled catalog and controlled world together. The first flag retains the two verified certus-growth routes. The second places four steam quarries facing north, east, south, and west, confirms them with the loaded shape matcher, and saves `planner-extraction/rotation-fixture.json`. Archive the stopped test world with `tools/extraction/archive_fixture.py`, then run `node tools/verify_rotation_world.mjs <world.zip> <catalog.json> <rotation-fixture.json>`. The runtime probe records exact world-state projections for directional shape rules by calling the loaded block rotation method and checking its inverse at two positions. `tools/extraction/verify_state_projection.py` records the four projection hashes in `data/provenance/state-projection-report.json`.

The recipe export reads both the recipe manager and every MI machine recipe provider. The latter supplies thousands of recipes absent from the manager, including generated canning and proxied smelting recipes. Recipe identity includes recipe type and ID because proxy recipes can share an ID with their source recipe. The TaCZ adapter records resolved ingredients and output components when that mod's serializer cannot encode its runtime recipe.

The released MI provider also mirrors every one of the 256 vanilla smelting and 4,322 stonecutting recipes into its furnace or cutting-machine type. `data/provenance/vanilla-export-report.json` checks each loaded source ID, ingredient alternatives, output count, EU, duration, lubricant, and corresponding supported catalog route. The furnace uses 2 EU/t for the source cooking time; cutting uses 2 EU/t for 200 ticks plus 1 mB lubricant. These are automatic machine routes, so the unsupported source entries do not mean their outputs lack industrial production. Of 144 loaded blasting recipes, 128 have an exact ingredient and output match among smelting recipes; 16 Spectrum blasting-only inputs remain distinct and need a verified furnace or blast-furnace adapter. Reproduce the audit with `python tools/extraction/verify_vanilla_exports.py --runtime <capture>/runtime.json --catalog data/statech-2.0.1.json.gz --output <report.json>`.

The machine probe creates detached block entities in the loaded server context and reads their actual components, capacities, batch limits, array eligibility, and serialized NBT. These are configuration probes, not functioning world machines. Batch energy probes use a known operation energy and the loaded transform. Arithmetic samples invoke the actual MI crafter method. Physical multiblock formation, throughput, and full warm-up behavior still require controlled world tests.

## Calculation findings

MI's base singleblock electric limit is 32 EU/t and its base overclock rate is 8 EU/t. Upgrades add to the limit. For a recipe with total energy E, recipe EU/t R, base B, maximum M, and efficiency counter e, the source computes `min(E, M, max(B, R) + floor(e * E / 600))`. The efficiency counter increases on completed recipes, not on every elapsed tick. The last tick consumes only the remaining energy. A planner must use whole ticks and the completion recurrence when estimating warm-up.

Tesseract's batching crafter transforms energy and base limits with the batch multiplier and discount, while adding upgrade energy separately. It casts floating-point transformed values to integers. Applying singleblock upgrade behavior independently to every internal machine would be wrong. The loaded Large Scale Assembler reports batch size 16, base 8 EU/t, base maximum 32 EU/t, and a 1,000,000 EU probe transforms to 13,600,000 EU for its full batch.

The two processing arrays accept different machine classes. The singleblock array accepts eligible electric singleblock machines; the multi-processing array accepts eligible MI electric multiblocks. Batching replacement machines do not automatically qualify for either. A controlled attempt to put the Large Scale Assembler into a multi-processing array was rejected by the actual mod class cast; do not expose that combination as valid.

The loaded MI Tweaks configuration has constant-efficiency hacks and casing-based overclock overrides disabled. Its redstone lock option is also disabled. Record these settings alongside extracted rules rather than assuming upstream defaults.

The machine probe also records primitive component fields and evaluates each loaded generator's fuel acceptance against the item and fluid registries. The fuel map's own enumeration method is unavailable for dynamic fluid fuels, so registry enumeration must call `accept` and `getEuProduction`. There are 12 captured generator configurations. The LV diesel generator supplies up to 64 EU/t and converts diesel at 800 EU/mB after its loaded multiplier; the large steam turbine supplies up to 16,384 EU/t and accepts four steam variants. The compact report retains the full verified fuel mappings. The bronze and steel boiler adapters include their loaded heater and solid-fuel rules. Other generator families still need their own process adapters.

## Remaining verification

The boiler probe runs the real heater and fuel-burning components in their normal tick order, records each change in steam output, and retains coal, water, steam, and cold-start deficit totals. The JavaScript boiler adapter matches every segment for both tested machines. This is a detached component test with continuous supplies and drained outputs, not a formed multiblock throughput test.

`python tools/extraction/normalize.py --capture <capture-directory> --output <private-output.json>` produces an intermediate capture format with version 1. It retains every source recipe, resolves item and fluid tag alternatives, preserves catalyst probabilities and process conditions, and reports unsupported ingredient and process adapters. The current reference capture yields 17,148 normalized processes and 9,333 explicitly unsupported entries. All 10,024 MI-style processes normalize. Component-bearing resources have stable identities; component predicates retain their captured matching display variants and the predicate itself. Those display variants do not establish every possible matching component combination. A normalized process is not a verified machine allocation: recipe-specific remainders, automation capacity, conditions, and machine-specific adapters must still be supplied. Keep this intermediate output private until the bundled dataset's redistribution audit and final format are complete.

The Java probe calls the loaded replication predicate, burn-time method, and crafting-remainder method for 11,573 default item stacks. It also decodes 202 distinct custom ingredient records through the loaded ingredient codec and verifies that each displayed stack satisfies its predicate. Both probes have zero failures. Default item metadata does not establish behavior for arbitrary component variants or recipe-specific remainder overrides. Loaded power conversion is 10 FE per EU and 2 FE per AE, with AE usage multiplier 1. The matching AE2 molecular assembler code ticks every game tick, has five acceleration-card slots, and charges the full final processing tick; its capacity evaluator includes that rounding.

The crafting probe inspects 7,301 loaded shaped and shapeless recipes. It tests a displayed ingredient combination, calls the actual assembly and remainder methods, and records the implementing class. Forty-six recipes need extra state or return an empty result with that combination; they remain explicitly unavailable. The iron-hammer plate recipe returns a hammer with 50 damage. Cake returns its three buckets. KubeJS's matching bytecode delegates ordinary remainders to the input stack when no ingredient action applies. The compiler permits the standard vanilla and KubeJS classes with unchanged results and no scripted ingredient action; component variants and tool damage still require separate adapters.

Run `python tools/extraction/player_dataset.py --input <normalized.json> --output <private-catalog.json>` to build the portable development catalog. It currently contains 28,558 process records and 9,333 separate unsupported source records. It supplies MI process rules, ordinary crafting through AE2, captured buffered generator fuels, bronze and steel boilers, and all three MI water pumps. `node tools/verify_real_catalog.mjs <private-catalog.json>` checks actual copper-cluster maceration, cake remainders, and the boiler/pump steam feedback through the shared solver. This catalog is still incomplete and private pending the full mechanics and redistribution audit.

A fresh-start repeated capture passed canonical comparison for all 26,481 recipes, resources, tags, and data maps. Both capture files had empty failure lists, and the machine report contained 211 entries. `data/provenance/runtime-report.json` records the counts, content digest, loaded mod versions, and arithmetic samples. The reference server emits unrelated optional-mod recipe warnings during startup and a configuration-unload exception during shutdown; the completed capture and its validation are separate from those pack log messages.

This capture does not establish full dataset coverage, redistribution approval for arbitrary mod assets, normalized process adapters, generation and closed-loop rules, complete upgrade compatibility, recipe conditions, warm-up buffers, or real-save reconstruction. No textures, mod jars, or worlds belong in the repository. Keep all unsupported recipe types and mechanics visible in the eventual dataset coverage report.

The water-pump probe temporarily places each actual pump in an empty area of the private test world and restores that area afterward. Bronze, steel, and electric pumps produce 1,000, 2,000, and 16,000 mB respectively every 100 ticks with eight counted water sources. Each consumes one unit of steam or EU per tick. A diagonal source counts only when an adjacent cardinal source exists; the probe verifies empty, isolated diagonal, paired, and full neighborhoods. The catalog exposes one through eight sources as explicit environmental configurations. Boiler startup stocks reserve actual cold-start fuel and water and the steam delivery deficit at the selected demand. These reserves include the initial water needed to bootstrap a steam-powered pump. Creative blaze cake cannot fuel a route until explicitly marked obtained.

The replicator tick probe produced one iron ingot on ticks 20, 40, and 60, consumed 300 mB of UU matter in total, and retained its one-item template. The catalog adds 11,257 default-stack replication routes accepted by the loaded predicate. Each is disabled in replicatorless mode and requires an obtained template plus one retained template per machine. Automatic planning of template acquisition is still pending; arbitrary component variants are not implied by default-stack checks.

Loaded large-electric-furnace coil tiers allow batches of 16, 32, and 64 at a 0.75 energy multiplier. Loaded pyrolyse tiers allow 1, 4, and 8 at multipliers 0.9, 0.8, and 0.75. Configuration validation applies the selected tier before batching and energy calculations. The tungstensteel furnace tier is a pack addition and is read from the loaded tier registry.

The dragon-egg siphon probe sets each loaded recipe on the actual crafter and calls its completion behavior against an energy output component. The two pack recipes release 102,400 and 204,800 EU, each after 100 internal progress ticks. Internal progress consumes no supplied electricity. The condition accepts an empty output buffer and rejects a full one. The adapter requires a single output hatch that can accept the complete burst; it also retains the fluid coproduct and the structure's dragon egg in the build requirements. Actual crafter ticks also verify both complete siphon cycles, including consumed inputs and fluid outputs. These are component checks with supplied inventories, not formed multiblock throughput tests. The pack removes arboreous-greenhouse recipes in its scripts, so the absence of that family in the effective recipe capture is expected.

The custom ingredient probe evaluates all 202 predicates against 12,319 candidate item stacks: default items, displayed ingredient variants, and component-bearing stacks encoded in effective recipes. It records 752 component-variant remainder samples. The catalogue uses only positively matched variants and labels the untested space of other component combinations. This enables the previously excluded component ingredient routes without assuming that display lists are exhaustive.

The loaded iron-plate hammer recipe consumes four ingots per plate. Repeated actual recipe remainder calls break iron, steel, diamond, and netherite hammers after 34, 87, 209, and 271 crafts. The probe also decodes each actual AE2 pattern and verifies that every damaged hammer remains valid with substitutions enabled. Separate tool-specific routes retain one working tool per assembler and amortize replacements over that lifetime. Enchanted tools and finite-job rounding are not implied by this steady-state rate.

All four MI large boilers now have cold-start schedules and a hot-running probe for every integer steam output from zero to full capacity. The probe calls the loaded heater and burner against constrained output slots, then reads the actual fuel-energy buffer change. The ordinary large boiler retains 205 EU/t of fuel use at zero output and reaches 256 EU/t at full output. High-pressure variants use eight EU per mB of steam. The catalog uses efficient periodic dispatch points with explicit buffering assumptions and accounts for fixed hot losses at partial load. Cold-start steam, water, fuel, and delivery deficits match the loaded components for all six boilers. Boiler routes include item fuel, loaded fluid fuels that fit the actual refill buffer, and ordinary or heavy-water variants at either supported pressure. Diesel-fueled heavy-water startup schedules also match all six loaded boilers. Fluid fuel keeps its own EU per mB and does not receive the item-fuel multiplier. These component checks do not establish formed-structure hatch capacity.

The pulse detonation generator uses the same verified burst-generation family. The probe runs all eight actual crafting cycles with their recipe inputs and an empty output-energy component. Completion ticks, consumed inputs, fluid coproducts, and energy match the loaded recipes, including the 24.576-billion-EU burst from the 64-nuke recipe. The calculator includes the fuel chains and reserves the full burst as startup energy stock when sustaining its average output from a cold start. A single output hatch must accept the entire burst; this avoids assuming that the mod's multiple-hatch insertion code distributes it correctly. Structural build bills and a formed-machine throughput check remain separate work.

The progression extractor reads the eight released FTB Quests chapter files through the loaded FTB Library SNBT parser. It records chapter IDs, file paths, and item-task IDs without treating quest rewards or icons as unlocks. Presets use cumulative item tasks and verified crafting conversions from already listed machines. Their source metadata records those conversions. Files are already covered by the extraction input hashes. The Electrical age preset includes the molecular assembler and basic MI upgrades; both processing-array types first appear in Quantum age. Unlisted entries remain available through individual overrides or the all-supported preset.

Initial graph categories are display hints derived from process types and resource names. They include extraction, ore processing, metals, chemicals, fuels, circuits, machinery, and power. These categories do not change recipe eligibility, costs, or calculations, and players can edit their workspace groups.

The probe reads both single shape templates and template arrays. This includes the four large boilers, whose structures contain 35 or 44 template cells before the controller. It records each hatch's empty slot capacities, fluid volume, energy buffer, and cable tier from the loaded objects. Examples include 15 slots in the highly advanced item hatch, 1,024,000 mB in its fluid counterpart, and 76,800,000,000 EU in a superconductor energy hatch. These are storage and cable-tier facts; transfer networks and a complete structural bill still need to be considered separately.

The probe also captures ten loaded EI and YAI integration data maps, photovoltaic cell components, and 15 nuclear fuel definitions. The three solar cells last 12,000 active ticks and have no minimum sunlight efficiency. These are extraction inputs; a complete solar production adapter is separate work.

The dedicated solar check places LV, MV, and HV panels in a clean isolated world. It uses their matching cells, then samples clear sky, distilled water, rain, and a roof after lighting settles. The loaded panels produced 32, 128, and 512 EU/t at clear noon, or 48, 192, and 768 EU/t while consuming one millibucket of distilled water per generating tick. Rain and a settled roof stopped output. A cell wears only while its panel is active; distilled water also halves wear by skipping every other active tick. The matching code limits ordinary generation to the daylight curve from world time 0 through 12,000. `data/provenance/solar-panel-report.json` records the jar and private-world hashes, measured samples, and nominal clear-day totals derived from that curve. Its ideal storage illustration is not a build bill.

Run `python tools/extraction/add_solar_routes.py --base data/statech-2.0.1.json.gz --report data/provenance/solar-panel-report.json --output <private-solar-catalog.json>` against a base catalog without solar routes, then pass that private file to `bundle_dataset.py` with the documented audit, extraction inputs, output, and manifest paths. The adapter checks the matching jar hash, report totals, resource IDs, and prior machine status. It adds dry and distilled-water profiles for all three tiers, each with an explicit cell supply rate, output-gap allowance, and initial-cell stock. The one-cell-per-day replacement budget is conservative: a dry panel sees 11,999 active ticks per day against a 12,000-tick cell life; water-assisted wear is slower. The connected planner uses measured MI storage transfer and capacity, not a constant solar average. It reports the clear-weather, open-sky, unobstructed-output assumption and the extra stored energy reserved for an unknown cell-gap phase. `node tools/verify_solar_catalog.mjs data/statech-2.0.1.json.gz` checks the bundled routes and an LV 14 EU/t plan. Recreate the source report from the private captured world before updating the catalog when the pack version changes.

Compile the probe with `python tools/extraction/build_probe.py --instance <instance> --jdk <jdk> --work <work>`, install its jar in the isolated instance, and use a fresh world when running `python tools/extraction/run_capture.py --instance <instance> --jdk <jdk> --solar-panel`. Archive the stopped world with `python tools/extraction/archive_fixture.py --world <world> --output <private-world.zip>`. Run `python tools/extraction/verify_solar.py --capture <instance>/planner-extraction/solar-panels.json --machines <instance>/planner-extraction/machines.json --jar <instance>/mods/extended-industrialization-1.16.2-1.21.1.jar --world <private-world.zip> --output data/provenance/solar-panel-report.json`. Keep the world, raw capture, and mod jar outside Git. The roof probe waits for lighting to settle; placing a roof and reading skylight in the same server tick gave a stale positive result in the initial trial.

The nuclear rod irradiator probe invokes the loaded absorption and energy routines with actual fuel items, neutron sources, and nuclear hatch inventories. All 60 combinations of fuel, source, and one/eight hatches match the captured irradiation rates and depleted outputs. Beryllium blocks supply 1,280 disintegrations per tick at 1,024 EU/t, with a 5% consumption check every 200 ticks. Demon cores supply 16,384 at 8,192 EU/t and take one damage every 50 ticks; the loaded 320-damage item lasts 16,000 ticks. The probe continues with empty hatches to verify ongoing wear and power. An exhausted source stack clears the source before that tick's power charge, making that tick free. Continuously refilled consumable stacks do not receive this saving; a worn-out source tool does. Beryllium consumption samples are random observations, not fixed yields. These component checks bypass structure linking and initial source discovery; formed-world startup remains a separate check.

The MI 2.5.8 reactor uses a spatial neutron and heat simulation rather than a fixed fuel-to-EU recipe. The loaded smallest structure matched 104 cells around its controller: 27 nuclear alloy pipe casings, 72 nuclear casings, one central item hatch, and four adjacent fluid hatches in the tested layout. With a quad uranium rod and water refilled after each tick, three component trials exhausted the rod on ticks 66,363, 66,382, and 66,487. They produced 398.6–399.2 million mB of steam and 63,082–63,704 mB of deuterium while consuming about 25 million mB of water. The highest observed hatch temperature was 1,769 °C, below the rod's 3,175 °C limit. A single uranium rod in the same central position produced 19,366 mB of steam over 20,000 ticks in one trial, far below the quad-rod output. The separate placed controller and matched hatches produced 24.8 million mB of steam in 5,000 ticks. These are stochastic samples for one hatch arrangement, with direct water refill and output draining. They cannot serve as exact production coefficients. Item, fluid, and power transfer are unconstrained in planning unless transfer is intrinsic to the process. `data/provenance/nuclear-reactor-report.json` records the input hashes, measurements, and limits. No reactor route is enabled yet.

To reproduce the reactor capture, compile the probe against the pinned instance, then run `python tools/extraction/run_capture.py --instance <instance> --jdk <jdk> --nuclear-grid --timeout 300`. Use `python tools/extraction/prepare_nuclear_fixture.py --machines <instance>/planner-extraction/machines.json --output <instance>/planner-structure-bill.json` before `run_capture.py --nuclear-structure --timeout 300`. Archive the stopped world with `archive_fixture.py`, then run `python tools/extraction/verify_nuclear.py --capture <instance>/planner-extraction/nuclear-grid.json --machines <instance>/planner-extraction/machines.json --formed <instance>/planner-extraction/structure-bill-check.json --jar <instance>/mods/Modern-Industrialization-2.5.8.jar --world <private-world.zip> --output data/provenance/nuclear-reactor-report.json`. The verifier pins the released jar and checks the loaded fuel definitions, geometry, coolant balance, full component lifetimes, and placed startup. Keep raw captures, the mod jar, and the world archive outside Git.

The released YAI script removes the arboreous greenhouse's crafting recipes and every process of that type. Its registered block and tier data remain visible in the raw capture, but it is not an available StaTech production route.

The Tesla controller probe tests all five loaded winding tiers with injected shape validity and two real energy components. Twenty idle ticks consume 1,280 / 5,120 / 20,480 / 81,920 / 327,680 EU. Loaded transfer limits are 1,536 / 6,144 / 49,152 / 393,216 / 6,144,000,000 EU/t; range is 32 / 64 / 128 / 256 / 512 blocks along each axis. An invalid shape consumes no power. An undersupplied valid tower drains the available energy but remains inactive. This probe establishes controller drain and loaded limits; it does not establish formed-world geometry, receiver transfer, or cable reachability. The portable infrastructure adapter uses these measurements.

The portable dataset now retains those Tesla measurements as infrastructure variants. Two copper towers add 128 EU/t; with 5 EU/t of other overhead they require 133 EU/t before generator support costs. Both desktop and browser use the same balance. Automatic world reconstruction remains separate work.

The energy-hatch probe ticks the loaded ElectricityNetwork with real hatch storage adapters and injected adjacency. All ten MI input/output hatches transfer their cable network limit over 20 ticks; adding a second network node leaves that shared limit unchanged. LV/MV/HV/EV/superconductor limits are 256 / 1,024 / 8,192 / 65,536 / 1,024,000,000 EU/t. Nominal tier values are recorded separately. These network measurements remain source evidence; factory structural bills use hatch storage and assume sufficient external transfer. The corrected eight-hatch irradiator with one MV input hatch matched the real shape and completed eight rods on tick 8,059 using 8,192,000 EU. The harness supplies its energy component directly; the separate network probe verifies the cable arithmetic.

The matching MI 2.5.8 storage-unit registration uses 100,000 times nominal cable-tier EU for capacity. A loaded one-node ElectricityNetwork with each real unit's insert and extract adapters transferred the full tier limit in both directions for 20 ticks. LV/MV/HV/EV/superconductor capacities are 3.2 million / 12.8 million / 102.4 million / 819.2 million / 12.8 trillion EU; transfer limits are 256 / 1,024 / 8,192 / 65,536 / 1,024,000,000 EU/t. `data/provenance/storage-unit-report.json` pins the jar, loaded capture, and private test world. Rebuild the probe and run the capture, then check it with `python tools/extraction/verify_storage.py --capture <capture>/machines.json --jar <instance>/mods/Modern-Industrialization-2.5.8.jar --world <private-world.zip> --output <report.json>`. These samples establish component and cable-network behavior, not a player's placed topology. The matching EnergyComponent saves `storedEu` and has no intrinsic decay in its insert and consume methods. Stored charge is a startup quantity, not a continuous power source.

A separate fresh world with `run_capture.py --storage-fixture` placed and saved all five units at one-third charge. `node tools/verify_storage_world.mjs <world.zip> <capture>/storage-fixture.json data/statech-2.0.1.json.gz <report.json>` confirms dedicated storage entries, positions, and exact `storedEu` quantities without recipe corrections. The compact result is `data/provenance/storage-world-report.json`. The bundled catalog merges the five measured storage rules into the verified rotation capture with `python tools/extraction/merge_storage_rules.py --base <rotation-normalized.json> --capture <storage-machines.json> --report data/provenance/storage-unit-report.json --output <merged-normalized.json>`, then runs `player_dataset.py` and `bundle_dataset.py`. The merge checks the private capture hash and changes only those five machine rules; it does not discard the earlier directional-structure evidence. The application imports saved counts into editable power settings and keeps saved charge separate from sustained generation.

A placed copper Tesla tower at (256, 100, 0) validates the calculated structure: 44 windings, 129 polished silver casings, 16 clean stainless steel casings, 16 stainless steel pipes, and seven LV energy hatches, plus the controller. Its own matcher accepted the geometry and linked the hatches. Twenty measured controller ticks after linking consumed 1,280 EU. This test supplies hatch energy directly and has no receiver. Run `node tools/prepare_tesla_fixture.mjs <catalog.json> F:/sti2-work/instance/planner-structure-bill.json`, install the rebuilt probe, and capture with `--structure-bill` to reproduce it. The other winding shapes use captured verified predicates and the same sizing algorithm; this fixture does not claim formed-world measurements for all five tiers.

The shape extractor resolves Tesseract's `PredicateSimpleMember` after checking its matching source and installed bytecode: it calls a `Predicate<BlockState>` and ignores the block entity. Every loaded state is tested. When all states of a block pass, one entry with empty `Properties` records that unrestricted block; otherwise the accepted property combinations remain explicit. This avoids repeating thousands of equivalent states while preserving the loaded predicate. The formed processing-array fixtures verify the captured glass members through the real shape matcher.

The certus probe checks all four budding-block variants through all four growth stages with controlled successful random choices. Flawless blocks remain intact; flawed, chipped, and damaged blocks each degrade one step on the forced wear branch. Actual filtered annihilation-plane pickup strategies reject immature buds. Mature clusters give four crystals for 6.5 AE, or one cluster for 28 AE with Silk Touch I. Powered storage insertion adds 1 AE per item. These results are recorded in `data/provenance/certus-growth-report.json`; reproduce the check with `python tools/extraction/verify_certus.py --capture <capture-directory> --output <report.json>`.

The accelerator component drains 80 AE from its local buffer per ten-tick call and declares 8 AE/t of network idle usage. These are separate mechanisms, not proof of a constant 16 AE/t demand: an empty local buffer cannot supply that second drain. Whole-network measurements must establish how the chosen power connection fills the buffer. The component report does not establish a complete farm's throughput or network cost.

The placed farm measurements in that report run each harvest mode for 600,000 real grid-service ticks. They use 24 connected glass cables, five planes, one filtered storage bus and chest, one accelerator, one energy cell, and one FE-fed energy acceptor. The resulting 33-node, six-channel network draws 15.546875 AE/t. The accelerator's local buffer remains empty. Every measured AE is accounted for by network idle plus 10.5 AE per plain harvest or 29 AE per Silk Touch harvest, including insertion. Recorded outputs are random observations; the planner uses the derived expected capacity of one cluster every 12 seconds across the five faces. Four successful growth stages, a one-in-five growth check, six direction choices, and one accelerator call every ten ticks establish that expectation. Natural random ticks are excluded. The planes respond to growth updates before the next accelerator call.

Rebuild the probe and run the capture with `--certus-farm` to reproduce both placed tests. The runner first loads the fixture chunks; AE2 storage buses deliberately reject targets in chunks that are loaded but not ticking. The fixture uses (64, 160, 0) in the private test world. It drives the actual grid services, supplies FE, and empties the chest each tick. It does not measure natural random ticks, output transport beyond the chest, or a player's chunk-loading setup. The planner requires a retained flawless quartz site and, for clusters, five already obtained Silk Touch I planes. It reports unknown stochastic startup timing while preserving those retained quantities.

Relative to the budding block, put the accelerator at (0, 0, 1). The cable ring consists of every (x, 0, z) with x and z from -2 through 2 where either absolute coordinate is 2. Add (2, y, 0) for y = -2, -1, 1, 2 and (x, y, 0) for x = 0, 1 and y = -2, 2. Put inward-facing planes at the five axis endpoints two blocks from the bud, excluding its south face. Add the south-facing storage bus at (0, 0, 2), the chest at (0, 0, 3), the cell at (1, 1, 2), and the acceptor at (1, 2, 2). Filter the bus to the final product only. This layout is a verified configuration, not a claim that its cable count is minimal.

Recipe display defaults prefer a unique output whose resource path exactly matches the recipe ID basename, provided that resource is not also an ingredient. Energy generation keeps electricity as its default. Other recipes retain their first captured output. This is a UI naming and initial-goal rule, not a change to quantities or a claim about exclusive production routes. It gives the pack's chloroform, diborane, and tetrafluoroethylene recipes their named products instead of their first-listed coproducts.

State-only shape predicates now omit a block property only when every loaded state with the same remaining properties has the same match result. The probe then checks all loaded states again against the projected patterns. The farmer predicate's 120,960 accepted states become 2,685 patterns; all 241,853 states of its matching block types were checked. Its JSON shrank from about 19 MB to 210 KB. data/provenance/state-projection-report.json records counts and hashes for 115 verified rules. Repeat the loaded capture, then run tools/extraction/verify_state_projection.py with --capture, --previous pointing to the prior dataset, and --output. The comparison also checks that every previously captured accepted pattern remains accepted.

Four machines remain registered but are excluded from automatic availability in the released pack: bronze and steel bending machines, the development supercomputer, and the development telescope. Their script classifications and absence of ordinary production recipes are checked by `tools/extraction/verify_availability.py` against the released archive. `data/provenance/machine-availability-report.json` records the archive and script hashes. Replication does not establish access without an obtained template. The catalog retains these machine definitions for explicit owned-machine overrides and world reconstruction.

The extractor records378 ordinary-crafting/MI-assembly pairs whose complete immediate inputs and outputs have exactly equal proportions. `tools/extraction/recipe_preferences.py` uses decimal rational arithmetic, so output batch sizes and split quantities are compared without binary rounding. It omits unsupported, conditional, ambiguous, catalytic, returned-container, tool, and probabilistic cases. These records come from the captured effective recipes; they do not infer equality between different upstream chains. Run `node tools/verify_recipe_preferences.mjs` to validate every pair and check real available, unavailable, and pinned assembly choices.

The loaded recipe audit found 16 Spectrum blasting inputs without an equivalent supported MI route. Build `planner-probe.jar`, prepare the private trial list with `python tools/extraction/prepare_blasting_fixture.py --runtime <runtime.json> --output F:/sti2-work/blasting-fixture.json`, and copy it to the isolated server as `planner-blasting.json`. Run the capture on a separate world with `--blasting-fixture`, then run `tools/extraction/verify_blasting.py` with `--base`, `--normalized`, `--runtime`, `--spectrum-jar`, `--fixture`, `--capture`, `--world-archive`, `--probe-jar`, and `--output`. `data/provenance/blasting-report.json` records their hashes and the matching Spectrum version. The probe places a real blast furnace, runs its loaded server tick, and checks all 16 output results after 100 ticks. Coal burned for 800 furnace ticks in every trial; one iron trial measured a lava bucket burning for 10,000 ticks and returning an empty bucket. The source archive and world stay outside Git.

For this pinned catalog, run `python tools/extraction/add_blasting_routes.py --base <rotation-storage-player-dataset.json> --normalized <rotation-storage-normalized.json> --capture <blasting.json> --output <private-catalog.json>`. This preserves every unrelated catalog entry while adding coal and lava routes for the 16 distinct sources. A full pack regeneration can instead pass `--blasting-report <blasting.json>` to `player_dataset.py`. The adapter retains a whole fuel item per furnace at startup. Its steady fuel rates are full-burn material equivalents under buffered operation, not a claim that an idle furnace uses fractional fuel. Other legal fuels are not verified. Run `node tools/verify_blasting_catalog.mjs <catalog.json>` and the dataset and distribution checks after rebuilding. The 128 blasting recipes that match smelting inputs and outputs remain represented by their supported generated MI routes; the remaining 16 now have explicit furnace routes.

The released Spectrum 1.12.7 Color Picker converts one item every five ticks while its ink storage has room. A fresh loaded-world fixture converted three brown dyes into 5, 10, and 15 brown ink by invoking the placed block entity's conversion logic; the five-tick cadence comes from the matching server-tick code. All 48 effective Color Picker recipes were read from the loaded recipe manager: dye, pigment, and pigment block routes for 16 colors. `data/provenance/spectrum-ink-report.json` records the matching jar, runtime, probe, private world, and trial hashes. Reproduce it with `run_capture.py --crystallarieum-fixture` in a separate isolated world, followed by `verify_spectrum_ink.py --runtime <runtime.json> --spectrum-jar <spectrum.jar> --capture <crystallarieum.json> --world-archive <world.zip> --probe-jar <probe.jar> --output <report.json>`. Then run `add_spectrum_ink_routes.py --base data/statech-2.0.1.json.gz --runtime <runtime.json> --capture <crystallarieum.json> --output <private-catalog.json>`, `bundle_dataset.py`, and `node tools/verify_spectrum_ink_catalog.mjs data/statech-2.0.1.json.gz`. Keep the world, jar, and full trial capture outside Git.

The same fixture established that a planted iron bud stops without an additive. With iron nuggets, it reached a large bud at tick 300 and a mature cluster at tick 600. Each trial consumed 240 brown ink, retained the 1,000 mB liquid-crystal fill, and produced a random 3–5 pure iron when harvested with a normal pickaxe. Nugget use was also random. The fixture explicitly harvested and reseeded the plant; it did not establish automatic harvesting, feed, or ink-network transfer. At that stage, Crystallarieum pure-resource growth remained unsupported; issue #49 tracked the missing automation and transfer evidence.

The follow-up `run_capture.py --spectrum-automation` fixture places a Color Picker, Crystallarieum, two connected ink nodes, and a separate AE2 grid with one Annihilation Plane, a filtered storage bus, chest, energy cell, and FE-fed energy acceptor. The Color Picker's verified five-tick conversion is driven directly while the isolated world is frozen; the placed Spectrum network transfers its ink. Dropped raw iron and iron nuggets reach the Crystallarieum through `fallOn`. A continuously active plane clears the small bud before growth, despite the output filter. The fixture therefore ticks the placed AE2 grid only after the mature cluster appears. Both controlled growth cycles mature at tick 600, consume 240 brown ink, and retain the 1,000 mB liquid-crystal fill. The staged plane collected four and three pure iron; pickup was delayed to tick 720 in one cycle. These are measured samples, not guaranteed yield or a complete autonomous farm. The same loaded server sampled ordinary-pickaxe harvest for all 20 effective Crystallarieum recipes, including the AE2 and Create integrations; their released loot tables specify 3–5 items. `data/provenance/spectrum-growth-report.json` pins the matching jar, runtime export, private capture, probe, and world archive. Rebuild the probe, run the fixture in the isolated server, archive the stopped world with `archive_fixture.py`, then run `verify_spectrum_growth.py --runtime <runtime.json> --spectrum-jar <spectrum.jar> --capture <spectrum-automation.json> --world-archive <world.zip> --probe-jar <planner-probe.jar> --output <report.json>`. That staged AE2 trial did not verify a player-buildable gate or its construction cost and sustained throughput.

A second controlled fixture, `run_capture.py --turtle-growth`, placed the loaded CC: Tweaked 1.120.0 normal turtle with its registered diamond-pickaxe upgrade two blocks above a Crystallarieum. The turtle pulled raw iron from the chest above, dropped it onto the machine, inspected each crop stage, dug only a mature cluster, and placed the ordinary pure-iron drops into the chest in front. The placed Color Picker and Crystallarieum accepted dye and nuggets through their side item handlers, and connected Spectrum ink nodes delivered 240 brown ink per growth. Two cycles reached maturity at tick 600, gave four pure iron each in this sample, and retained the 1,000 mB liquid-crystal fill. The stationary turtle used no movement fuel. `data/provenance/turtle-growth-report.json` pins the matching Spectrum and CC: Tweaked jars, runtime export, private capture, probe, and stopped-world archive. Rebuild the probe, run the isolated trial, archive the world, and run `verify_turtle_growth.py --runtime <runtime.json> --capture <turtle-growth.json> --world-archive <world.zip> --probe-jar <planner-probe.jar> --spectrum-jar <spectrum.jar> --cc-jar <cc-tweaked.jar> --output <report.json>`. The fixture chooses commands directly while its world is frozen; it has not run an autonomous Lua program or measured its scheduling overhead. The two chests are supplied as startup stock rather than by built support chains. That direct-command fixture did not establish autonomous scheduling or a complete route.

The unpaused follow-up, `run_capture.py --turtle-lua`, copied `tools/extraction/fixtures/spectrum_turtle.lua` into the placed turtle's own computer and turned it on. Chests and hoppers fed two raw-iron starters, brown dye, and iron nuggets. The running Lua program inspected the crop, harvested mature clusters, and put seven sampled pure iron into its output chest over two cycles. A server-tick observer recorded harvests at ticks 633 and 1,262 after startup, so the observed repeat interval was 629 ticks. The computer remained on with zero turtle movement fuel, the two ink stores totaled 160 after 128 dyes generated 640 ink and two growths consumed 480, and the 1,000 mB liquid-crystal fill remained. `data/provenance/turtle-lua-report.json` pins the loaded jars, runtime export, probe, Lua program, private capture, and stopped-world archive; the archive also contains the tested startup program. Reproduce it by building the probe, running `run_capture.py --turtle-lua --timeout 300` in the isolated server, archiving the stopped world, and running `verify_turtle_lua.py --runtime <runtime.json> --capture <turtle-lua.json> --world-archive <world.zip> --probe-jar <planner-probe.jar> --spectrum-jar <spectrum.jar> --cc-jar <cc-tweaked.jar> --startup tools/extraction/fixtures/spectrum_turtle.lua --output <report.json>`. One autonomous iron trial does not establish a guaranteed yield, all 20 growth recipes, upstream feeds, or the full farm construction bill. That one Lua trial did not yet establish the source-derived variants and full construction bill described below.

The current catalog adds 60 additive variants from the 20 loaded Crystallarieum recipes, replacing their unsupported entries. `tools/extraction/spectrum_growth.py` resolves the released input tags and uses the matching Spectrum code: each one-second step advances `int(20 × acceleration)` stage ticks; the recipe's ink-cost tier is squared, then multiplied by acceleration and the additive's ink modifier; one additive is consumed at its recorded chance per step. The adapter rounds each stage up to a whole step. All verified mature-cluster loot tables yield three to five items, so the planner balances an expected four per crop and labels that expectation. The iron Lua farm took 629 ticks between outputs, 29 beyond its 600-tick growth rule. Each configuration reserves 40 turtle ticks beyond its own derived growth schedule, a visible estimate for the other variants rather than a guaranteed throughput bound. The matching block entity has a 1,000 mB tank and tests its fluid without draining it. The full tank appears as retained startup stock, not a recurring fluid input. Color Picker configurations now include their feeder and source ink node; each farm configuration includes its Crystallarieum, turtle, pickaxe upgrade, input and output chests, additive feeder, and destination node. Shared Color Pickers may feed several farms on a connected network.

To update the bundled routes, verify the private growth and Lua reports first, then run `add_spectrum_growth_routes.py --base data/statech-2.0.1.json.gz --runtime <runtime.json> --growth-report data/provenance/spectrum-growth-report.json --lua-report data/provenance/turtle-lua-report.json --output <private-catalog.json>` against a catalog without these routes. Regenerate `distribution-audit.json` with `audit_distribution.py --instance <isolated-instance> --dataset <private-catalog.json> --output`, then run `bundle_dataset.py --source <private-catalog.json> --output data/statech-2.0.1.json.gz --audit data/provenance/distribution-audit.json --inputs data/provenance/statech-2.0.1-inputs.json --manifest data/provenance/distribution-manifest.json`. Check `node tools/verify_spectrum_growth_catalog.mjs data/statech-2.0.1.json.gz`, the Godot growth UI test, and the browser Worker with `--growth`. The tested Lua program names iron stages and output; the other variants use the shared loaded machine behavior and loot rules but have not all run through a separate autonomous farm. Their timing and probabilistic supplies remain visibly estimated, and the wider dataset stays incomplete.
